# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  hello: "Hello world"
  api:
    slack:
      help: | 
        *Easily Manage and Preview Your Stylesheets*

        `/aphrodite stylesheets/index` List All Stylesheets
        `/aphrodite stylesheets/:id/export` Download your custom variable file 
        `/aphrodite stylesheets/:id/variables` List Variables for a Specific Stylesheet
        `/aphrodite stylesheets/:id/preview` Get a preview link to your styles 
        `/aphrodite stylesheets/:id/publish` Make your stylesheet live (if permitted)
        `/aphrodite variables/:id <new value>` Update a Variables Preview Value
